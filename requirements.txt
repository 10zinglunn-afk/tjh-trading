# Core harness — everything in run.py / sanity_check.py needs only these two.
pandas>=2.0
numpy>=1.24

# Optional, NOT needed to run the harness on synthetic or cached CSV data:
#   yfinance>=0.2     # only for fetch_data.py (pull real SPY/QQQ locally)
#   torch, huggingface_hub  # only for a REAL forecast_kronos.py run (not --mock)
